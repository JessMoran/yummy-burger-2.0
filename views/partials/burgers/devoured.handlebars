<li class="m-3 flex justify-between">
  <span class="my-auto text-left"> {{ this.id }}. {{ this.burger_name }}</span>
  <button class="devoured shadow bg-black hover:bg-yellow-700 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 rounded ml-3"
          data-id="{{this.id}}">
    Devour it!
  </button>
</li>
