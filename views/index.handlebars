<div class="grid grid-cols-2 divide-x divide-black w-1/2 pr-12">
  <div class="text-center">
    <h2 class="text-black font-bold text-xl text-center">Devoured</h2>
    <ul class="mb-20 mt-5 mx-6 text-left">
      {{#each burgers }}
        {{#if devoured}}
          <li class="m-3 flex justify-between">
            <span class="my-auto text-left"> {{ this.id }}. {{ this.burger_name }}</span>
          </li>
        {{/if}}
      {{/each}}
    </ul>
  </div>

  <div class="text-center">
    <h2 class="text-black font-bold text-xl text-center mb-8">Waiting area</h2>
    <div class="text-center divide-y divide-black">
      <ul class="mb-20 mt-5 mx-5 text-left">
        {{#each burgers }}
          {{#unless devoured}}
            {{> burgers/devoured }}
          {{/unless}}
        {{/each}}
      </ul>

      <form class="w-full max-w-sm pt-12 create-form">
        <div class="md:flex md:items-center mb-6">
          <div class="md:w-1/3">
            <label class="block text-black font-bold md:text-right mb-1 md:mb-0 pr-4" for="burger-name">
              Add burger
            </label>
          </div>
          <div class="md:w-2/3">
            <input class="bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-yellow-500"
                   id="burgerName" type="text" value="Bacon burger" name="burger">
          </div>
        </div>
        <div class="md:flex md:items-center mb-6">
          <div class="md:w-1/3"></div>
          <div class="md:w-2/3">
            <button class="shadow bg-black hover:bg-yellow-700 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 rounded"
                    type="submit">
              Submit
            </button>
          </div>
        </div>
        <div class="md:flex md:items-center">
          <div class="md:w-1/3"></div>
          <span class="text-sm px-4">
            Enter a burger you want to eat!
            **Click the submit button
          </span>
        </div>
      </form>

    </div>
  </div>
</div>
